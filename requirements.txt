# Student Result Management System
#
# No third-party packages are required -- the project intentionally uses
# only the Python standard library (csv, array, os, sys, unittest) so it
# runs anywhere Python 3.9+ is installed with zero extra installation steps.
